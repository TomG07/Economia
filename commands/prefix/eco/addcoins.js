const Discord = require("discord.js");
module.exports = {
  name: "addcoins",
  aliases: ["addmagias", "setmagias"],
  run: async(client, message, args, prefix) => {
    let p = prefix || "ny!";
  }
}
